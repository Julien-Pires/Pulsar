namespace Pulsar
{
    /// <summary>
    /// Represents the methods that will be called when a transform changed
    /// </summary>
    /// <param name="transform">Transform</param>
    public delegate void TransformChangedEventHandler(Transform transform);
}